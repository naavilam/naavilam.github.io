name: Render workflow sequence diagrams (SVG)

on:
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  render:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python deps
        run: |
          python -m pip install --upgrade pip
          pip install pyyaml

      - name: Docker version
        run: docker --version

      - name: Render SVGs
        run: |
          bash .github/scripts/render_all.sh

      - name: Ensure drawio folders exist
        run: |
          mkdir -p assets/drawio
          mkdir -p .tmp/drawio-export
          mkdir -p assets/svg

      - name: Export Draw.io (.drawio) -> SVG (to temp)
        uses: rlespinasse/drawio-export-action@v2
        with:
          path: assets/drawio
          format: svg
          output: .tmp/drawio-export
          remove-page-suffix: true
          embed-svg-fonts: true
          svg-theme: light

      - name: Copy Draw.io SVGs into assets/svg (keep folders)
        run: |
          set -euo pipefail
          # copia mantendo a mesma hierarquia de pastas do export
          rsync -a --prune-empty-dirs \
            --include '*/' --include '*.svg' --exclude '*' \
            .tmp/drawio-export/ assets/svg/

          echo "=== exported drawio svgs ==="
          find assets/svg -type f -name '*.svg' | sed 's#^# - #' || true

      # - name: Debug workspace
      #   run: |
      #     pwd
      #     ls -la
      #     ls -la .github || true
      #     ls -la .github/scripts || true

      - name: Commit & push generated diagrams (if changed)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add assets/svg assets/mmd || true

          if git diff --cached --quiet; then
            echo "No diagram changes to commit."
            exit 0
          fi

          git commit -m "chore(diagrams): update diagrams"
          git push